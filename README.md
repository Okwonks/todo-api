# Todo API

A nodejs CRUD API

## Dev

### Requirements

* postgresql
* [sequelize](https://sequelize.org/)
* yarn

### Startup

1. yarn && run `npx sequelize db:create` (creates dev db `todo_dev`)
2. start server

 yarn dev
